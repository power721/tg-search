# Release Notes v0.22.0

## 优化

- **公共搜索不再卡顿**：搜索读取改走独立的只读数据库连接池，不再与写入排队；新增组合索引。后台同步消息时，搜索响应从约 11s 恢复到毫秒级。
- **资源统计刷新更省资源**：不再对每条新消息做全表统计重算，改为后台最多 5 秒一次的合并刷新，活跃频道下的 CPU 占用明显下降。
- **消息缺口补齐更稳健**：单次缺口补齐运行增加 5 分钟上限，避免个别卡住的请求长时间占用同步队列；补齐失败的频道进入冷却（默认 90s），不再反复重试制造无效任务风暴。
- **123 网盘链接检测提速**：所有 123 链接统一走单个规范域名，连接可复用。30 条新格式分享链接的检测从约 4.5s 缩短到约 0.24s（约 18 倍）。
