# Release Notes v0.24.0

## 修复

- **管理后台接口不再长时间卡顿**：修复消息同步、盘检等写入任务繁忙时，服务状态、任务列表、资源统计、链接分组等管理接口排队数十秒才响应的问题。读查询改走独立的只读连接池，不再与写入争抢同一数据库连接；会话校验同样受益，重负载期间所有管理端接口保持流畅。
- **仪表盘统计改为快照展示**：资源分类统计与链接类型分组不再每次实时全表扫描（大库下单次即可达数十秒），改为内存快照立即返回，后台自动刷新（最多延迟一分钟），服务重启后自动预热，打开首页始终秒开。
