# Release Notes v0.20.0

## 新增

- 链接检测运行时设置：设置页新增「链接检测」分区，可在线调整网盘链接检测（`/api/check/links`）的检测并发数与结果缓存时长，保存后立即生效，无需重启。

## 优化

- 网盘链接检测性能大幅提升：`/api/check/links` 改为并发检测，并复用进程级 HTTP 客户端（连接池 + HTTP/2 多路复用），300 条链接可在亚秒级完成。
- 新增确定性结果缓存，避免短时间内重复请求同一链接时反复打满上游接口。
